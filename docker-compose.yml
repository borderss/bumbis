version: "3"
services:
  app:
    container_name: new_project_app
    build: .
    ports:
      - "80:80"
    image: vue3_new_project:latest
    environment:
      # GIF picker providers — leave unset to disable that tab's provider.
      - GIPHY_API_KEY
      - KLIPY_API_KEY